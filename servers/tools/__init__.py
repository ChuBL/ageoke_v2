"""
MCP tool implementations.

  ingestion      — Docling PDF → Markdown
  extraction     — Instructor + schema registry → structured JSON
  mindat_matcher — Mindat API mineral/rock normalization
  geosciml_matcher — GeoSciML vocabulary term matching
"""
